mode: ContinuousDelivery
tag-prefix: '[vV]?'
branches:
  main:
    regex: ^main$
    increment: Minor
  feature:
    regex: features?[/-]
    increment: Minor
  hotfix:
    regex: hotfix(es)?[/-]
    increment: Patch
  release:
    regex: ^releases?[/-]
    increment: Patch
ignore:
  sha: []
